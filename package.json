{
  "name": "amplitude-proxy",
  "version": "1.0.0",
  "main": "serve.js",
  "repository": "https://github.com/navikt/amplitude-proxy.git",
  "author": "NAV",
  "license": "MIT",
  "scripts": {
    "serve": "nodemon",
    "mocha": "mocha",
    "up": "docker-compose up -d --remove-orphans --build",
    "down": "docker-compose down"
  },
  "dependencies": {
    "fastify": "^2.8.0",
    "fastify-formbody": "^3.1.0",
    "json-schema-library": "^4.1.0"
  },

  "devDependencies": {
    "nodemon": "^1.19.2",
    "dotenv": "^6.2.0",
    "mocha": "^6.0.2",
    "request": "^2.88.0",
    "request-promise-native": "^1.0.7",
    "wait-on": "^3.2.0"
  }
}
